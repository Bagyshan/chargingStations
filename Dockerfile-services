FROM eclipse-temurin:21.0.2_13-jdk-jammy AS build

ARG JAR_FILE
WORKDIR /build

ADD $JAR_FILE application.jar
RUN java -Djarmode=layertools -jar application.jar extract --destination extracted

FROM eclipse-temurin:21.0.2_13-jdk-jammy

# OTel Java-агент (Phase 3): авто-инструментация трейсов (WebFlux, WebClient, Kafka,
# R2DBC/JDBC, Redis, Spring Security) без правок кода. Подключается через -javaagent
# в JAVA_TOOL_OPTIONS (см. x-java-opts в docker-compose.prod.yaml). Скачивается демоном
# на этапе сборки; chmod 0644 — чтобы non-root пользователь spring-boot мог читать jar.
ADD https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases/download/v2.6.0/opentelemetry-javaagent.jar /otel/opentelemetry-javaagent.jar
RUN chmod 0644 /otel/opentelemetry-javaagent.jar

RUN addgroup spring-boot-group && adduser --ingroup spring-boot-group spring-boot
# Каталог для загружаемой статики (иконки коннекторов station-controll-service).
# Создаём с правами spring-boot заранее, чтобы docker-том, смонтированный сюда,
# унаследовал владельца и приложение (не-root) могло писать в него новые файлы.
RUN mkdir -p /application/uploads && chown -R spring-boot:spring-boot-group /application
USER spring-boot:spring-boot-group
VOLUME /tmp
WORKDIR /application

COPY --from=build /build/extracted/dependencies .
COPY --from=build /build/extracted/spring-boot-loader .
COPY --from=build /build/extracted/snapshot-dependencies .
COPY --from=build /build/extracted/application .

ENTRYPOINT exec java ${JAVA_OPTS} org.springframework.boot.loader.launch.JarLauncher ${0} ${@}