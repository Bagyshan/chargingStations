server:
  port: 8006

spring:
  application:
    name: notification-service

  mail:
    host: smtp.yandex.ru
    port: 587
    username: flagman-inc@yandex.ru
    password: garzkehjpethfhye
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: notification-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: false
      auto-offset-reset: earliest
      max-poll-records: 50
    listener:
      type: batch  # или single, но batch удобнее для производительности
      concurrency: 3  # количество параллельных потоков обработки

# Топики
kafka:
  topics:
    notification-events: notification.events

# Настройки приложения
app:
  base-url: http://localhost:8005  # базовый URL для ссылок подтверждения
  verification:
    email:
      from: flagman-inc@yandex.ru

logging:
  level:
    charg.ing.stations: DEBUG
    org.apache.kafka: WARN